export interface IStoryCard {
  /** The unique identifier. */
  id?: string;
  /** Additional CSS class names. */
  className?: string;
  /** The URL for the link. */
  linkUrl: string;
  /** The heading text. */
  heading: string;
  /** The image URL. */
  imgUrl: string;
}
